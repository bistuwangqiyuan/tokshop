/**
 * Chinese edition of the paid handbook. See handbook.en.ts for why this lives
 * in a TypeScript module. {{PRICE_TABLE}} is substituted at delivery time.
 */

export const HANDBOOK_ZH = `# 开源大模型 API 选型与成本优化实战手册

版本 1.0 · tokshop.xyz

---

## 关于这本手册

这是一份来自生产系统的工作文档，不是行业综述。文中描述的一切都跑在 tokshop.xyz 上：
一个 OpenAI 兼容的 API，转售开源模型、按 token 计费、在两个大洲收款，并且自动生产
自己的 SEO 内容。

凡出现数字，要么来自各家公开的价目，要么来自你可以自己重算一遍的算术。凡有不确定
之处，本手册会直说，而不是猜一个。这里没有编造营收数字的案例研究 —— 一旦编造，其余
内容也就一并失去价值了。

花掉这一美元之前，请先知道三件事：

1. 这本手册讲的是**单位经济模型与计费正确性**，不讲 prompt 技巧。如果你想找提示词
   方法论，这笔钱不该花在这里。
2. 代码模式是 Serverless 环境下的 TypeScript 加 Postgres。推理过程可迁移到任何技术栈，
   但具体代码片段以此栈为前提。
3. 价格附录在你下载的那一刻生成，数据源与对外公开的价目接口完全相同。

---

## 第一章 唯一能经受现实检验的定价模型

### 1.1 对转售方来说，为什么按量计费优于订阅

订阅制把「你收多少」和「你欠上游多少」解耦了。当你的边际成本接近零时，这没问题；
当你的边际成本是别人开给你的按 token 账单时，这是灾难 —— 一个重度用户可以在一个下午
消耗掉一个月的收入。

按 token 计费让两者始终耦合：每一美元成本，都由一美元多的收入触发。为这份安全感付出的
工程代价是：你的计量必须时刻正确，包括请求在中途被中断的时候。第三章就专门讲这件事。

### 1.2 零售倍率

最简单且站得住脚的模型，是在上游成本上乘一个固定倍率：

    零售价 = 上游价格 × 倍率

tokshop.xyz 取 **1.5**，对应 token 收入的 33.3% 毛利率：

    毛利率 = (1.5 - 1) / 1.5 = 0.333

**先定倍率，再定价格档位**，因为倍率决定了某个价格档位能不能扛住支付手续费。一个常见
错误是先挑好看的整数零售价，事后才发现毛利吸收不了支付通道的抽成。

固定倍率有两个性质，值得为它的简单买单：

- **可审计。** 客户可以拿你的公开价格与上游的公开价格对比，看清加价幅度。藏起加价幅度
  只会招来「实际情况恐怕更糟」的猜测。
- **自动纠偏。** 上游降价时（开源模型提供方降价很频繁），你的零售价随之下降，毛利率
  保持不变。

### 1.3 你实际在卖什么

你卖的不是 token。token 是商品，而且上游卖得比你便宜。你卖的是上游不打包提供的四样东西：

- **一套凭证打通多个模型。** 换模型从「建立一段新的供应商关系」变成「改一个字符串」。
- **有硬上限的预付支出。** 预付余额不可能产生意外账单。对个人开发者或做演示的人来说，
  这比几个百分点的价差更值钱。
- **无起充门槛、无合同。** 多数上游有最低消费。如果你没有，这就是一个真实的产品特性。
- **可审计的计费记录。** 每次调用的 token 数与费用，即时可见。

如果你说不清自己在卖上面哪一样，那你的加价就是任意的，一轮价格战就会把它抹掉。

---

## 第二章 支付手续费决定你的价格档位

本章包含全书最有用的一段算术，也是最常被跳过的一段。

### 2.1 在低价位上，固定手续费就是全部

支付手续费分两部分：按比例的一部分，和每笔固定的一部分。在高客单价上，比例部分主导，
固定部分是噪声；在低客单价上，结论完全反转。

以一家收 **3.9% + $0.40** 的记录商户为例：

| 订单 | 手续费 | 占订单比例 |
| --- | --- | --- |
| $1 | $0.439 | 43.9% |
| $3 | $0.517 | 17.2% |
| $5 | $0.595 | 11.9% |
| $20 | $1.180 | 5.9% |
| $100 | $4.300 | 4.3% |

再叠加 1.5 倍零售倍率 —— 金额为 \`A\` 的订单所售额度，上游成本是 \`A / 1.5\`：

    净利 = A - 手续费(A) - A/1.5

| 订单 | 手续费 | 上游成本 | 净利 |
| --- | --- | --- | --- |
| $1 | $0.439 | $0.667 | **-$0.106** |
| $3 | $0.517 | $2.000 | +$0.483 |
| $5 | $0.595 | $3.333 | +$1.072 |
| $20 | $1.180 | $13.333 | +$5.487 |
| $100 | $4.300 | $66.667 | +$29.033 |

一美元充值**每卖一笔亏一笔**。而且不是小亏：这在结构上无法通过调参修好，因为手续费加上
上游成本本身就已经超过了收入。

### 2.2 盈亏平衡价位

设比例费率为 \`p\`、固定费为 \`f\`、倍率为 \`m\`，解净利为零的那个点：

    A - (p·A + f) - A/m = 0
    A · (1 - p - 1/m) = f
    A_min = f / (1 - p - 1/m)

代入 p = 0.039、f = 0.40、m = 1.5：

    A_min = 0.40 / (1 - 0.039 - 0.6667) = 0.40 / 0.2943 = $1.36

所以 $1.36 是下限。低于它的一切都是营销费用，就该按营销费用做预算。

这条公式就是本章的全部。在公布价目表之前，把你自己的数字代进去算一遍。注意它的一个
推论：**提高倍率会降低你的最低可行价格** —— 这就是「对小额订单收更高加价」在经济上
诚实的论据。

### 2.3 为什么第二条通道会改变答案

不是所有通道都有固定费。中国的钱包通道 —— 通过服务商接入的支付宝与微信支付 —— 通常
是纯比例：钱包官方约 0.38% 到 0.6%，服务商 1% 到 2%，全包约 2.6%，且**没有固定部分**。

同一笔一美元订单重算一次：

| 通道 | $1 订单手续费 | 上游成本 | 净利 |
| --- | --- | --- | --- |
| 卡组织，3.9% + $0.40 | $0.439 | $0.667 | -$0.106 |
| 钱包，2.6% 纯比例 | $0.026 | $0.667 | **+$0.307** |

同一个商品、同一个价格，在一条通道上亏钱，在另一条通道上有健康毛利。而按盈亏平衡公式，
\`f = 0\` 会让 \`A_min\` 变成零：在纯比例通道上，根本不存在最低可行价格。

由此得到的实操结论：

- **微价格商品属于纯比例通道。** 如果你的低价引流商品只能通过带固定费的卡通道触达客户，
  那它是一则广告，不是一个商品。
- **按地域路由，而不是按你的偏好路由。** 提供客户所在地能用的通道。不要试图把所有人赶到
  对你最便宜的那条上 —— 一次支付失败的成本是 100%。
- **每条通道单独建模。** 混合平均费率会掩盖掉那个正在亏钱的分段。

### 2.4 容易被忘掉的成本

- **提现费。** 一笔固定提现费（比如 $7，或按 1% 取高）对 $50 的提现是 14%，对 $700 的
  提现是 1%。攒大批次、少提现；钱放在哪边都不生息。
- **汇率转换。** 如果收款币种与结算币种不同，那么转换点差一定发生在某个环节。在被它意外
  之前，先搞清楚它在哪里。
- **争议与拒付。** 一笔拒付通常在退款之外再收一笔固定争议费。一次拒付就能抹掉几十笔小额
  订单的毛利 —— 这才是「退款邮件要在一天内回复」的真实原因。
- **失败重试。** 有些通道对拒付授权也收费。

### 2.5 记录商户，还是不是

记录商户（Merchant of Record）成为法律意义上的销售方。它在全球代收代缴 VAT、GST 与
销售税，你无需在从未去过的国家做税务登记。你用比例费率为此付费。

支付服务商（PSP）只搬钱。税务登记、开票与合规仍然归你。

对一个面向全球销售的个人经营者，记录商户的溢价几乎总是值得的，理由与钱无关：跨境 VAT
搞错的失败模式不是「利润少一点」，而是「几年后才发现的一笔负债」。这份保险该买。

但要精确区分你的哪条通道属于哪种。通过技术服务商接入的境内钱包通道通常**不是**记录商户，
经它取得的收入是你自己的经营所得，需你自行申报。把这两者在账上混为一谈，是这门生意里
最常见的会计错误。

---

## 第三章 不会丢钱的计量

API 转售方只有一个不可饶恕的 bug：你交付了一次响应，却没有计费。本章讲它发生的三个地方。

### 3.1 存钱要留足小数位

一次小请求的成本可能不到万分之一美元。输入单价 $0.27 / 百万 token 时，100 个 token
的成本是 $0.000027。

余额与费用要用**八位小数**存，而不是两位：

    balance  numeric(16,8)
    cost     numeric(16,8)

两位小数会把上面那次调用四舍五入成零。一个用户发一百万次小请求可以一分钱不付，而你要等
上游账单来了才会发现。凡是钱，一律用精确十进制类型，绝不用浮点。

费用表达式本身平平无奇：

    费用 = 输入 token × 输入单价每百万 / 1e6
         + 输出 token × 输出单价每百万 / 1e6

### 3.2 余额闸门要放在调用上游之前

转发前检查余额，余额耗尽时用 HTTP 402 拒绝。402 是正确的状态码 —— 它就是为这件事存在的 ——
客户端会把它与 401 区别对待。

有一个值得想清楚再决定的设计点：闸门判 \`余额 > 0\`，而不是 \`余额 >= 预估费用\`。你在调用
前无法知道费用，因为输出长度未知。所以一个以正余额开始的请求会被允许跑完，可能把余额压成
轻微负数。另一种做法 —— 预先冻结一个估算额、事后对账 —— 是为了避免每用户几分钱的敞口而
搭一整套机械。那几分钱就认了吧。

不过要在条款里写明这一点，让负余额永远不是意外。

### 3.3 流式响应是漏钱的地方

非流式很简单：响应体里带 usage，照它计费，结束。

流式有两个坑。

**坑一：usage 块是可选的。** 在 OpenAI 的流式协议里，只有请求明确要求了，流才会报告
token 用量。如果你的客户没带那个选项，你收不到 usage，也就不会计费。

修法是别再信任客户端。转发前重写请求：

    body.stream_options = { ...body.stream_options, include_usage: true }

这样无论调用方发了什么，用量都会回来。就这一行，区别在于「对全部流式流量计费」还是
「只对恰好主动开启该选项的那部分计费」。

**坑二：结算得太晚。** 如果你等客户端把流完整消费完再计费，那么在 90% 处断连的客户端
永远不会触发结算 —— 而上游已经生成了完整回复并已向你收费。

要在流转换器的 **flush** 阶段结算 —— 它在上游流结束时运行，而不是在客户端读完时运行。
结构上：

    transform(chunk) {
      如果这个 chunk 带 usage 就捕获
      原样透传
    }
    flush() {
      结算(已捕获的 usage)   // 上游完成时运行
    }

顺序很关键：transform 阶段捕获，flush 阶段结算，且绝不让结算依赖客户端的行为。

### 3.4 记录每一次调用，并让客户能读

每次调用记录：模型、输入 token、输出 token、实收费用、是否流式、时间戳。在控制台里
立即可见。

这不是锦上添花。它是把一场计费争议变成一分钟对话的那个凭据，而按 token 计价只有在可核查时
才值得信任。它同时也让你自己的毛利分析成为可能：没有分模型的用量数据，你无法判断哪些模型
值得继续挂着。

### 3.5 API Key：哈希存储，并且认真对待这件事

创建时展示一次。存哈希和一小段用于展示的前缀。绝不存 Key 本身。

后果是你真的无法找回丢失的 Key，只能吊销重发。把这一点写在界面上。任何能把 Key 邮件发给
你的系统，都是一个「数据库泄露即全面失守」的系统。

每次请求按哈希查表，并检查状态字段，让吊销立即生效而不是最终一致。

---

## 第四章 收钱而不丢账

支付 bug 比计量 bug 更糟，因为它对客户可见，而且涉及的钱已经动过了。

### 4.1 在离开自己的系统之前先建单

订单行先以 \`pending\` 状态建好，然后才把客户送到支付方的页面。把你的订单号交给支付方
作为它的引用号。

这个顺序意味着：任何一笔真实发生过的支付，都有一个可以落地的地方。反过来 —— 等回调到了
才建记录 —— 会丢掉所有回调格式异常、延迟到达、或恰好在你部署期间到达的支付。

它同时给了你一个对账面：超过一小时仍为 pending 的订单，恰好就是值得排查的那一批。

### 4.2 绝不自己渲染支付表单

每条通道都跳转到支付方的托管页面。客户在支付方的域名上填卡号或钱包凭证，你的服务器从不
接触它们。

这不是偷懒，是缩小范围。卡数据一旦经过你的基础设施，你的合规义务就变了。托管页面还自带
钱包支持 —— Apple Pay 与 Google Pay —— 否则你得逐平台实现和维护；而且它们的转化率通常
比你自己做的更好，因为支付方在所有商户上做过 A/B 测试。

### 4.3 两层幂等，因为回调一定会重复

每一家支付方都会重复投递回调。有的在超时后重试，有的在非 2xx 时重试，有的就是会重试。
按至少一次（at-least-once）投递来假设。

**第一层：把事件 ID 唯一地记下来。**

    unique (provider, event_id)

用 \`on conflict do nothing\` 插入。如果插入没产生行，说明这个事件见过了，确认并停止。注意
唯一键包含 provider，这样两家 ID 方案冲突的支付方不会互相遮盖。

**第二层：让状态迁移带条件。**

    update orders set status = 'paid'
     where id = $1 and status = 'pending'

\`and status = 'pending'\` 是承重部分。只有一次执行能命中，所以同一订单的两个不同事件仍然
只入账一次。这一层覆盖了第一层覆盖不了的情况：不同的事件 ID 描述同一笔支付。

任一层单独都不够。两层都要做。

### 4.4 没有事务也要原子结算

「把订单标记为已付」和「给余额入账」是两次写入，绝不能分开。如果进程死在中间，订单显示
已付，而客户什么都没拿到。

基于 HTTP 的 Serverless Postgres 驱动往往没有交互式事务，跨两次往返的 \`BEGIN ... COMMIT\`
用不了。答案是用 CTE 把两次写入表达成**一条语句**：

    with paid as (
      update orders set status = 'paid', paid_at = now()
       where id = $1 and status = 'pending'
      returning user_id, credits, kind
    ),
    credited as (
      update users u set balance = u.balance + p.credits
        from paid p
       where u.id = p.user_id and p.kind = 'credits'
      returning u.id
    )
    select * from paid;

一条语句就是一个事务。第二层的条件更新仍然在里面，所以这同时做到了原子与幂等。它还只有
一次网络往返，恰好比它替换掉的两次更快。

注意是 \`balance = balance + credits\`，而不是读出余额、算好、再写回。跨往返的
读-改-写会丢掉并发入账；让数据库做这个加法，它就丢不了。

### 4.5 验签，并对弱签名做二次校验

签名方案的强度不一样，你的处理方式也应该体现这一点。

**对原始报文做 HMAC** 是强的那种。在解析之前，对收到的确切字节计算摘要。两个常被做错的
细节：

- 用**恒定时间比较**，并先做长度判断 —— 恒定时间比较函数在长度不等时通常会抛异常。
- 对**原始**报文签名。如果你先解析再重新序列化，键顺序与数字格式都会变，摘要就对不上了。

**按参数排序拼接后 MD5（共享密钥）** 是弱的那种，在境内支付通道里仍然常见：参数按键排序、
拼接、末尾接上密钥、取哈希。它能鉴别身份，但离「可被猜测」更近，而且密钥是与第三方共享的。

对弱签名，要加一道检查：**在入账之前，向支付方回查订单状态并核对金额。**

    如果 远端状态 != 已付: 确认接收，不入账
    如果 |远端金额 - 我方应收金额| > 0.01: 拒绝

这把重放或伪造的回调变成了空操作，因为攻击者还得同时让支付方相信这笔订单已付。任何你无法
独立确认的回调，都应被当成「去查一下」的提示，而不是「付款」的指令。

### 4.6 严格返回支付方期望的内容

支付方从你的响应里推断是否成功。有的期望 JSON 200；有的期望响应体就是字面量 \`success\`，
看到别的会重试六次。

读规范，一字不差地照做。然后有意识地利用它：当失败发生在你这一侧且是暂时性的，就**故意**
返回非成功响应，让重试发生。支付方的重试循环是免费的持久性，但前提是你没有因为把失败也
确认掉而不小心把它关掉。

### 4.7 上线前先把冲正路径做完

几乎所有人都实现了支付然后就停了。然后一笔退款来了，客户既拿走了钱，也留住了额度。

要让退款与败诉的争议，与支付路径对称地处理：

    with reversed as (
      update orders set status = 'refunded'
       where id = $1 and status = 'paid'
      returning user_id, credits, kind
    ),
    debited as (
      update users u set balance = greatest(u.balance - r.credits, 0)
        from reversed r
       where u.id = r.user_id and r.kind = 'credits'
      returning u.id
    )
    select * from reversed;

这里嵌了两个决定：

- \`greatest(..., 0)\` 把余额下限压在零。如果客户已经花掉了正在被追回的那部分，差额由你吸收，
  而不是给他造一个永远清不掉的负余额。这是为了让账号保持可用而做的有意选择，损失有上限
  且很小。
- 对于「授予访问权」类商品，状态变更本身就是全部的权限回收，因为每一次访问检查都要求
  \`status = 'paid'\`。把权限建模为「存在一笔已付订单」，绝不要建模成一个需要额外同步的布尔字段。

---

## 第五章 微价格商品的游客结账

在一美元的购买之前强制注册，会流失掉大部分买家。但余额需要一个账号来承载。解法是把两类
商品区别对待。

### 5.1 哪类商品可以免账号

- **余额充值需要账号。** 否则额度无处安放；任何「无账号」方案本质上都是一个体验更差的账号。
- **授予访问权的商品不需要。** 一笔授予下载权的购买，只需要给买家一个能回来的持久办法。

### 5.2 把订单行的用户列改为可空，就是全部的表结构改动

    user_id uuid null references users(id)
    email   text            -- 始终写入

游客订单有邮箱、没有用户。当有人注册或登录时，把这些孤儿绑定上去：

    update orders set user_id = $1
     where user_id is null and lower(email) = lower($2)

在注册和登录两处都跑。注册时它接住「先买后注册」；登录时它接住「未登录状态下以游客身份
购买」—— 后者比你以为的更常见，因为人们会换一个浏览器来买。

匹配要忽略大小写。邮箱的输入方式五花八门，这里一次不匹配就会悄无声息地藏掉某人的购买。

### 5.3 一份权限，三种凭证

没有账号时，那笔已付订单**就是**权限本身。按强度递减接受多种证明：

1. **会话** —— 其账号拥有该商品的已付订单。
2. **签名访问 Cookie** —— 在交付时下发，内含订单号。签名过，所以无法伪造；是 Cookie，
   所以重复下载无需动脑。
3. **兑换码** —— 买家长期保留的那份凭据。

兑换码的字母表里去掉 \`0\`、\`O\`、\`1\`、\`I\`、\`L\`。兑换码会被念出来、从截图里重敲、粘贴时
带上尾随空格；字母表里的歧义最终都会变成客服邮件。

兑换码是持有即有效的令牌，所以要对每单的交付次数设上限。上限设得让任何诚实买家都碰不到，
同时又低到「码被公开发帖后会失效」，就够了。别为一份一美元的文档做 DRM —— 那点收益换不来
它给真实客户增加的摩擦。

### 5.4 交付页与回调竞态

支付方一重定向，买家立刻回到你的站点，而 webhook 可能还没到。所以交付页有时会被要求渲染
一个仍是 \`pending\` 的订单。

不要靠信任重定向来糊过去。把两个问题分开：

- **「这位访客是不是发起了这笔结账？」** 用你自己放在回跳 URL 里的签名令牌回答。除非你能
  从规范中核验支付方自带回跳签名的算法，否则不要把这件事建在它上面 —— 无法核验就无法依赖，
  而对这个用途，你自己控制的 HMAC 一样好用。
- **「这笔钱付了没有？」** 只从订单行回答。那是唯一真相来源，由经过验签的回调写入。

当令牌有效而订单仍为 pending 时，就明说，并自动刷新。若支付方提供状态查询接口，在这里调
一次还能把「回调丢了」变成自愈的情形。

---

## 第六章 按「每个有效答案的成本」选模型

比较每百万 token 的单价，是在比较错误的数字。

### 6.1 真正重要的那个数

你真正关心的是：

    每有效答案成本 = 每次调用成本 / 成功率

其中「成功」由你的任务定义：JSON 解析通过了、分类分对了、摘要不需要重做。一个价格只有一半
但成功率 70% 的模型，比贵一些但成功率 95% 的模型更贵：

    便宜的：1.00 / 0.70 = 1.43
    贵的：  1.80 / 0.95 = 1.89   —— 此时贵的仍然更差
    便宜的：1.00 / 0.50 = 2.00   —— 现在贵的胜出

用你自己的成功率算一遍。排序会在很合理的取值处翻转 —— 这正是「按每 token 单价比较」会
误导人的原因。

### 6.2 输出单价的主导作用比你以为的更强

输出 token 通常是输入 token 的数倍，而对生成类任务，你付钱买的就是输出。把两个价格公布成
一个数字，能藏起三倍的差距。

比较之前，先测你自己的输入输出比。一个「长 prompt、一个词答案」的分类器，几乎完全由输入
定价；一个摘要器几乎完全由输出定价。对前者更便宜的模型，往往对后者更贵。

### 6.3 定型之前该测什么

- **在你自己数据上的任务成功率。** 五十个真实样本，对你的具体用途胜过任何公开榜单。榜单
  测的是一个不属于你的分布。
- **延迟分布，不是均值。** 用户体感的「慢」是 p95。均值好但长尾差的模型，比一个各处平庸的
  模型体感更糟。
- **真实可用上下文。** 宣传的窗口是「模型不再报错」的位置，不是「模型不再退化」的位置。
  在你实际使用的长度上测检索准确率。
- **压力下的行为。** 高峰期的限流与排队是价格的一部分。支付方的失败模式也是：它是排队，
  还是直接丢？

### 6.4 让切换保持廉价

你接入的每一个模型都应该是表里的一个标识符，而不是一条代码分支。把对外 slug 映射到上游 ID，
把价格当数据存，用一个开关标记模型上下架。

这样加模型是插一行，改价是一次 update，下架是一个布尔值。这件事在开源模型服务领域比在别处
更重要，因为前沿以周为单位移动：你今天接入的模型，会在你为它写的任何代码需要改动之前就被
取代。

---

## 第七章 把带客户来的内容自动化

开发者 API 没有病毒传播回路。流量来自搜索某个问题的人，并且越来越多地来自代其作答的 AI
助手。两者可以由同一批内容服务，但前提是这些内容是为机器抽取而写的。

### 7.1 永远答案优先

搜索摘要与助手回答都在抽取简短、自足的段落。把每篇文章都写成可抽取的结构：

- 开头一段 **TL;DR**，用两三句话回答标题里的问题，并且在被无上下文引用时依然成立。
- **问句式标题**，因为问题就是这样被提出来的。
- **短段落**，三句左右，每段只讲一个点。
- 结尾一个 **FAQ**，既真的有用，也能直接被消费为结构化数据。

要用的检验标准是：如果机器把页面里任意一个段落单独摘出来，它是否仍然真实且有用？如果它
需要上一段才讲得通，它就会被错误引用。

### 7.2 机器可读的接口很便宜，而且值得做

- \`robots.txt\` 应显式允许你希望被读取的那些 AI 爬虫。沉默是有歧义的，有些爬虫也确实把它
  当成歧义处理。
- \`llms.txt\` 给出站点提供什么的紧凑、富链接摘要。
- 文章、FAQ、面包屑、商品与报价的结构化数据，全部与可见页面同源生成，这样两者不会漂移。
- 发布时主动推送即时收录，而不是等着被爬。

### 7.3 生成式内容需要的是事实核查闸门，不是文风闸门

如果你用模型生成文章，真正会伤到你的失败不是文笔笨拙，而是一个被自信地写错的价格。

所以质检闸门必须对照你自己的数据库核查事实，而不只是看可读性。具体做法：把草稿里每一处
价格声明抽出来，逐条在实时价目库里查，任何一条不符就拒稿。

这是内容流水线里价值最高的一百行代码。一个把自家价格写错的博客，比没有博客伤害更大 ——
它把一个访客转化成了「认为你对数字很随意」的人，而这恰恰是他要信任你不会犯的那件事。

再加上结构性检查：TL;DR 在不在、有没有 FAQ、标题是不是问句。结构是可机械检查的，那就机械
地检查它。

### 7.4 做翻译，并且把 hreflang 做对

如果你的市场是双语的，就把两种语言作为独立 URL 发布，配互指的 \`hreflang\` 标注和正确的
\`lang\` 属性。技术文章的机器翻译如今已经足够好，边际成本接近零，而触达大致翻倍。

真正会出错的不是翻译，是标注。每个语言版本都必须指向所有其他版本，包括自己；canonical 必须
指向它自己所在的那个版本，而不是英文原版。

---

## 第八章 运维的最低限度

一些无聊、便宜，但正是小服务能持续在线的原因的事。

**凡有钱流动处，皆需幂等。** 不只是 webhook。任何可能被重试的接口，都应该重试安全。

**每个事实只有一个真相来源。** 价格在数据库里，商品定义在一个模块里，所有页面都从它们读。
小型电商站点最常见的 bug，就是一个价格被硬编码在三处，其中两处过期了。如果你发现自己在
改同一个数字两次，这个 bug 已经存在了。

**对生产环境跑自动化端到端测试。** 不是对你的 mock 做单元测试，而是对线上系统发真实请求，
检查结账有响应、webhook 会拒绝错误签名、重复回调只入账一次、被篡改的金额会被拒。每次部署
都跑。

**告警要盯对账缺口，而不是错误率。** 危险状态是安静的：永远没结算的 pending 订单，或者
记了用量但余额没动。定时统计这些。

**法务页面要早于支付开户。** 服务条款、退款政策、隐私政策是支付账号的前置条件，不是事后
补的文书。写得足够具体以至于真实。一份承诺「你的代码确实会做的事」的退款政策，比一份你
兑现不了的大方政策更值钱。

**两条通道，各自可独立开关。** 任何单一支付方都可能在很短通知期内暂停你。如果每条通道都由
配置独立启用，失去一条只是减少结账选项，而不是停止销售。

---

## 结语

这些都不难。它是几百行仔细写的代码，加上一段在定价之前该做的算术。它之所以有价值，是因为
这些失败模式都很安静：一个不计费的流、一次入账两遍的回调、一个每卖一笔亏一角的一美元商品。
它们都不会把你叫起来。它们都会累积。

如果这本手册你只带走一样东西，就带走 2.2 节那条盈亏平衡公式，在公布价格之前用你自己的
数字算一遍。

问题与纠错都真心欢迎：support@tokshop.xyz。如果你发现这里有错，告诉我们，我们会修正
文档 —— 买家免费获得更新。

---

## 附录：实时价目表

下表在你下载这份文件的那一刻从生产数据库生成，因此与公开 API 此刻返回的内容一致。权威来源
始终是 \`GET https://tokshop.xyz/v1/models\`。

{{PRICE_TABLE}}

---

*《开源大模型 API 选型与成本优化实战手册》版本 1.0，由 tokshop.xyz 销售。授权购买者本人使用，
请勿二次分发。纠错与更新对买家免费 —— 随时用兑换码取回当前版本。*
`;
